# Лабораторна работа №5 / Вариант 1 
### Условие задачи
Генератор случайных чисел в заданном диапазоне. Не используйте готовые реализации ГПСЧ. Отфильтруйте вывод генератора, чтобы он не содержал чисел с числом делителей больше n.
К генератору должна быть применена хотя бы одна из функций map, reduce, filter.

Функция для подсчёта числа элементов в списках, включая вложенные списки:


**Скрипт выполняет следующие шаги:**
1. Генерирует псевдослучайные числа в заданном диапазоне с использованием линейного конгруэнтного генератора (LCG).
2. Подсчитывает количество делителей для каждого числа.
3. Фильтрует числа, оставляя только те, у которых количество делителей не превышает заданного значения.

* def simple_random(min_val, max_val) - реализация ГПСЧ с использованием линейного конгруэнтного метода.
  ``` python
  def simple_random(min_val, max_val):
    seed = simple_random.seed
    seed = (1664525 * seed + 1013904223) & 0xFFFFFFFF #число не должно превышать 32 бит
    simple_random.seed = seed
    return min_val + (seed % (max_val - min_val + 1))
  ```
* def count_divisors(num) - функция, которая считает количество делителей.
  ``` python
  def count_divisors(num):
    if num < 1:
        return 0
    count = 0
    for i in range(1, int(num**0.5) + 1):
        if num % i == 0:
            count += 1
            if i != num // i:
                count += 1
    return count
  ```
* numbers - список, с ранодмными значениями
* filtered_numbers - фильтрация списка numbers по условию count_divisors(x) <= n


#### Вывод:
```
Сгенерированные числа: [78, 33, 48, 27, 14, 89]
После фильтрации: [33, 27, 14, 89]
```


## Список используемой информации
1. [🐍 Самоучитель по Python для начинающих. Часть 13: Рекурсивные функции - proglib.io](https://proglib.io/p/samouchitel-po-python-dlya-nachinayushchih-chast-13-rekursivnye-funkcii-2023-01-23)
2. [Как работает рекурсия – объяснение в блок-схемах и видео - Хабр](https://habr.com/ru/articles/337030/)

